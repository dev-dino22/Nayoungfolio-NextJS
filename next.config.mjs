// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
    compiler: {
        styledComponents: true, // styled-components SSR을 지원하도록 설정
    },
};

export default nextConfig;
